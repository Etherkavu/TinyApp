<!DOCTYPE html>
<html lang="en">
<head>
  <% include ../partials/_header %>
</head>
<body>

  <ul>
    <p>This is the index file.</p>

    <% for (var key in urls) { %>
        <li><%= key %> : <%= urls[key] %></li>
    <% }; %>
  </ul>

</body>
</html>